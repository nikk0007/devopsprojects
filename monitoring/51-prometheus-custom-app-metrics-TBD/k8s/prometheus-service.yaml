apiVersion: v1
kind: Service
metadata:
  name: prometheus-service
spec:
  selector:
    prometheus: my-prometheus
  ports:
    - name: web
      port: 9099
      targetPort: web
  type: NodePort  # Adjust the type as needed
