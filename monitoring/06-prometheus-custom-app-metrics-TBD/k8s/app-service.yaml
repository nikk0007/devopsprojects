apiVersion: v1
kind: Service
metadata:
  name: my-python-app-service
spec:
  type: NodePort
  selector:
    app: my-python-app
  ports:
  - name: http
    protocol: TCP
    port: 8000
    targetPort: 8000
